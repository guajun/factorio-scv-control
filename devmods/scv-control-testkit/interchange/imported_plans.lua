-- Host tooling replaces this data-only catalog inside an isolated TestKit copy.
-- A developer may also generate it here, then reload the interactive test save.
return {protocol = "scv-navigation/1", kind = "solver-bundle", cases = {}}
